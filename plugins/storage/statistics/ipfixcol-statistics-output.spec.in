Summary: Statistics storage plugin for ipfixcol.
Name: @PACKAGE_NAME@
Version: @PACKAGE_VERSION@
Release: @RELEASE@
URL: http://www.liberouter.org/
Source: http://homeproj.cesnet.cz/rpm/liberouter/stable/SOURCES/%{name}-%{version}-%{release}.tar.gz
Group: Liberouter
License: BSD
Vendor: CESNET, z.s.p.o.
Packager: @USERNAME@ <@USERMAIL@>
BuildRoot: %{_tmppath}/%{name}-%{version}-%{release}

BuildRequires: gcc autoconf libtool make doxygen libxslt pkgconfig @BUILDREQS@
Requires: libxml2 rrdtool ipfixcol >= 0.7.1
BuildRequires: libxml2-devel rrdtool-devel ipfixcol-devel >= 0.7.1

%description
Statistics storage plugin for ipfixcol.


%prep
%setup

%post
ipfixconf add -c "%{_sysconfdir}/ipfixcol/internalcfg.xml" -p o -n statistics -t statistics -s "%{_datadir}/ipfixcol/plugins/ipfixcol-statistics-output.so" -f

%preun

%postun
ipfixconf remove -c "%{_sysconfdir}/ipfixcol/internalcfg.xml" -p o -n statistics

%build
%configure --with-distro=@DISTRO@
make

%install
make DESTDIR=$RPM_BUILD_ROOT install

%files
#storage plugins
%{_datadir}/ipfixcol/plugins/ipfixcol-statistics-output.*
%{_mandir}/man1/ipfixcol-statistics-output.1.gz
