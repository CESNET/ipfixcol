
bmgen tool generates bitmap file (VAH so far, should be COMPAX in near future) (work stoped)
The output of the bmgen can be then read by bmread tool. (work stoped)

compaxgen generates COMPAX bitmap file (first working implementation (dirty))
compaxread reads COMPAX file (first working implementation (dirty))

ozrgen generates bitmap file with zero words reductions (first working implementation)
ozrread reads 'OZR' bitmap file (first working implementation)



Test files
==========

Example list of dst/src ports from records collected by NFDUMP is available
on cellar machines in directory /home/data/groups/test/ipfixcol/bitmaps/.
There are examples of 5min time window and 1hour time window. These files
are supposed to be an input files of bmgen tool.

